{
  "scripts": {
    "soiac": "soiac",
    "pip-install": "python3 -m pip install -r requirements.txt",
    "build": "npm run soiac && npm run pip-install",
    "snippets": "npm run build && python3 snippets.py",
    "start-service": "npm run build && python3 start_service.py",
    "call-service": "npm run build && python3 call_service.py"
  },
  "devDependencies": {
    "soia-python-gen": "^1.0.6",
    "soiac": "^1.0.27"
  }
}
